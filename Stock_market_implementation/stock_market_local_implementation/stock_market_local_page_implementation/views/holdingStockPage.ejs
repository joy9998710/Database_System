<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Holding Stock Page</title>
</head>
<body>
    <% if(typeof searchValue !== 'undefined' && searchValue === 'fail') { %>
        <script>alert('Can not find matching stock')</script>
    <% } %>
    <form method="post">
        <input type="text" name="stockName">
        <button type="submit">검색</button>
    </form>
    <table>
        <tr>
            <th><label>종목명</label></th>
            <th><label>매수가</label></th>
            <th><label>현재가</label></th>
            <th><label>보유수량</label></th>
            <th><label>거래가능수량</label></th>
            <th><label>평가손익</label></th>
            <th><label>등락률 (%)</label></th>
        </tr>
        <% stocks.forEach(stock => { %>
            <tr>
                <td><%= stock.stock_name %></td>
                <td><%= stock.buying_price %></td>
                <td><%= stock.current_price %></td>
                <td><%= stock.holding_num %></td>
                <td><%= stock.transaction_available %></td>
                <td><%= stock.Sonik %></td>
                <td><%= stock.up_down %></td>
            </tr>
        <% }) %>
    </table>
</body>
</html>